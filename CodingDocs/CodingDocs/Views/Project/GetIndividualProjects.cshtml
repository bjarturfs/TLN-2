@model List<CodingDocs.Models.ViewModels.ProjectViewModel>

@{
    ViewBag.Title = "GetIndividualProjects";
}

<h2>IndividualProjects</h2>

<ul>
    @foreach(var proj in Model)
    {
        <li>@proj.Name</li>
    }
</ul>